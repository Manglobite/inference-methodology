# Хронология кейса <MODEL>

> Шаблон. Фиксируйте фазы по фактам (даты, прогоны, решения). Негативные
> результаты и отменённые планы сохраняются, а не удаляются. Расчётные величины
> помечайте явно; итоговые выводы опирайте на измеренные значения.

## Фаза 1. Скачивание весов — <дата>

- Источник: `<HF-repo>`, commit `<commit>`.
- Скачаны файлы, сверены по размеру и sha256:
  - `<file>` — `<bytes>` Б, sha256 `<sha256>`.

## Фаза 2. Подготовка стенда — <дата>

- ЗАПОЛНИТЬ: остановка прод-бэкенда, снятие idle-политики, проверка GPU/диска.

## Фаза 3. Smoke-прогоны — <дата>

| Прогон | Профиль | Квант | Карта(ы) | KV | Пик VRAM | Каталог |
| --- | --- | --- | --- | --- | ---: | --- |
| | | | | | | `results/<id>` |

## Фаза 4. Принятая раскладка — <дата>

ЗАПОЛНИТЬ: какие профили/кванты приняты и почему (чтобы внутри пары сравнения
отличалось только исследуемое измерение).

## Фаза 5. Запланированные прогоны (план) — <дата>

ЗАПОЛНИТЬ: план измерительных прогонов. Если план отменён — пометьте это и
сохраните раздел как след.

## Фаза 6. Канонические прогоны — <дата>

ЗАПОЛНИТЬ: список канонических прогонов (`results/<id>`), статусы, ключевые
числа. Источник — `docs/results-tables.md`.

## Методические решения

1. ЗАПОЛНИТЬ: выравнивание пар сравнения.
2. ЗАПОЛНИТЬ: условия, при которых замеры сопоставимы.
3. ЗАПОЛНИТЬ: параллельные волны и контроль искажений (если были).

## Негативные результаты

ЗАПОЛНИТЬ: что не подтвердилось и было сохранено намеренно.
