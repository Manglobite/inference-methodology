# <MODEL> на <HARDWARE>: <короткая суть исследования>

**Русский** | [English](README.en.md)

> Шаблон. Замените все `<...>`-плейсхолдеры и заполните разделы фактами.
> Пометки «ЗАПОЛНИТЬ» — что именно требуется. Не оставляйте плейсхолдеры в
> публикуемой версии.

## Цель

ЗАПОЛНИТЬ: какую гипотезу проверяет исследование и что должен уметь
воспроизвести читатель. Например:

1. как ведёт себя **<MODEL>** при контексте **<CTX>** на стенде **<HARDWARE>**;
2. как меняются prefill/decode при заполнении контекста;
3. что даёт <варьируемый фактор>.

## Модель

| Поле | Значение |
| --- | --- |
| Модель | `<MODEL>` |
| Источник GGUF | `<HF-repo>` (HF), commit `<commit>` |
| Файл(ы) | `<file>.gguf` — `<bytes>` Б, sha256 `<sha256>` |
| Архитектура | ЗАПОЛНИТЬ (слои, attention-типы, GQA, head_dim) |
| Нативный контекст | `<CTX>` |
| Лицензия | ЗАПОЛНИТЬ |

Веса в репозиторий **не включаются**: воспроизводятся только URL, размер и
sha256 (см. [RUNBOOK.md](RUNBOOK.md), [LICENSE-NOTICE.md](LICENSE-NOTICE.md)).

## Стенд и софт

- GPU: `<HARDWARE>`;
- CPU/RAM/ОС/драйвер/тулчейн: см. [HARDWARE.md](HARDWARE.md);
- рантайм: `<RUNTIME>`, движок/commit/флаги — в [RUNBOOK.md](RUNBOOK.md).

## Методика

ЗАПОЛНИТЬ: режимы прогонов (smoke/ladder/A-B), фиксация генерации (seed,
`temperature: 0`, `ignore_eos`, `n_predict`), привязка устройств, сбор
телеметрии. Общая методика — в
[METHODOLOGY.md](../../METHODOLOGY.md).

> Ссылка `../../METHODOLOGY.md` корректна **для скелета**
> (`methodology/templates/research-case/`). **После копирования кейса в
> `inference_cases/<case>/`** путь меняется на `../methodology/METHODOLOGY.md`.
> Сверьте ссылку после копирования; при иной раскладке замените её на стабильный
> URL/commit методики (тег), чтобы ссылка не выглядела битой и читатель не
> зависел от структуры исходного дерева.

## Варианты

| # | Профиль | Веса | KV | Устройство | Роль |
| ---: | --- | --- | --- | --- | --- |
| 1 | `<profile>` | ЗАПОЛНИТЬ | ЗАПОЛНИТЬ | ЗАПОЛНИТЬ | ЗАПОЛНИТЬ |

## Результаты

ЗАПОЛНИТЬ: сводная таблица по каноническим прогонам. Источники —
`docs/results-tables.md` (человекочитаемые таблицы) и `docs/results.json`
(машинно-читаемая сводка со списком канонических/контрольных прогонов);
сырые прогоны — `results/<run_id>/`. Канонический результат — агрегат по
**n ≥ 3** успешным прогонам (**медиана + min/max**, с указанием `n` и списка
`run_id`); при `n < 3` результат помечается ограниченным/неканоническим.
Норма — в [METHODOLOGY.md](../../METHODOLOGY.md) §11.1 (для скелета;
после копирования кейса путь — `../methodology/METHODOLOGY.md`).
Производные величины (проценты, Δ) помечайте как **вычислено**,
интерпретацию отделяйте от фактов.

## Графики

ЗАПОЛНИТЬ: ссылки на `docs/figures/*.svg`. График обязателен **при наличии
соответствующей измеренной серии**; если серия не измерялась — график не
строится, а причина фиксируется здесь (см.
[METHODOLOGY.md](../../METHODOLOGY.md) §11.2 — для скелета; после копирования
кейса путь — `../methodology/METHODOLOGY.md`).

## Негативные результаты

ЗАПОЛНИТЬ: что проверяли и что не подтвердилось (сохраняется намеренно).

## Ограничения

ЗАПОЛНИТЬ: что не измерялось, при каких условиях числа несопоставимы.

## Воспроизведение

Кратко (полный порядок — в [RUNBOOK.md](RUNBOOK.md)); скрипты предварительно
скопированы из `methodology/scripts/<категория>/` в `scripts/` (см.
[scripts/README.md](scripts/README.md)):

```bash
# 1) предполёт: свободные GPU, диск, снятие idle-политики узла
# 2) smoke-проверка профиля
# 3) прогоны (ladder / A-B)
# 4) агрегация: таблицы и графики
```

## Структура репозитория

```text
<case>/
├── README.md / README.en.md
├── HARDWARE.md
├── RUNBOOK.md
├── CHRONOLOGY.md
├── LICENSE-NOTICE.md
├── study.json (optional)
├── profiles/
├── prompts/
├── scripts/
├── data/
├── docs/
└── results/
```

## Публикация

ЗАПОЛНИТЬ после прохождения барьера: `scripts/check-public.sh` (ожидается
`OK: no private markers found`), санитайз публикационной копии
`scripts/sanitize-results.sh --in-place` (оба скрипта скопированы из
`methodology/scripts/publish/`). Санитайз с `--in-place` деструктивен: применяйте
его к публикационной копии дерева или предварительно сделайте архив/бэкап
исходных `results/`. Барьер покрывает `/home/<user>/`, `/root/`,
`/Users/<user>/`, `/mnt/`, RFC1918, `sk-[A-Za-z0-9_-]{13,}`, `Authorization: Bearer` и
`api_key`/`secret`/`token`; имена хостов baseline не ловит — их добавляют через
`PUBLIC_DENY_FILE`. В публикуемых текстах — только относительные пути;
абсолютные домашние пути, имена хостов и внутренние IP не публикуются.
Веса модели не коммитятся.
