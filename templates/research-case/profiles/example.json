{
  "_note": "Syntactically valid JSON TEMPLATE, not ready to run: replace every <...> placeholder and delete this _note. The example shows a two-GPU topology -- verify it against your own rig. Key fields: name (must match the file name without .json); engine; model_path (relative to the repo root); served_model_name (API alias); binary (path to the server binary); build_variant (manifest key); gpu {pci_bus_id, cuda_visible_devices, llama_device}; ctx_size; parallel; cache_type_k/cache_type_v; generation fields seed/temperature/ignore_eos/n_predict (fixed for comparability); port; command_env; command (the actual argv, <repo> is the repo-root placeholder) and the mode flags below. The full actual command is the source of truth: everything else here is a human-readable summary of it.",
  "name": "<profile>",
  "engine": "llama.cpp",
  "model_path": "data/models/<MODEL>-<quant>.gguf",
  "served_model_name": "<model-alias>",
  "binary": "tools/llama-cpp/<RUNTIME>/bin/llama-server",
  "build_variant": "<RUNTIME>",
  "gpu": {
    "pci_bus_id": [
      "00000000:<bus-a>:00.0",
      "00000000:<bus-b>:00.0"
    ],
    "cuda_visible_devices": "0,1",
    "llama_device": "CUDA0,CUDA1"
  },
  "ctx_size": 262144,
  "parallel": 1,
  "cache_type_k": "q8_0",
  "cache_type_v": "q8_0",
  "seed": 42,
  "temperature": 0,
  "ignore_eos": true,
  "n_predict": 128,
  "port": 18091,
  "command_env": {
    "CUDA_DEVICE_ORDER": "PCI_BUS_ID",
    "CUDA_VISIBLE_DEVICES": "0,1"
  },
  "command": [
    "<repo>/tools/llama-cpp/<RUNTIME>/bin/llama-server",
    "-m",
    "<repo>/data/models/<MODEL>-<quant>.gguf",
    "-a",
    "<model-alias>",
    "--device",
    "CUDA0,CUDA1",
    "--split-mode",
    "layer",
    "--tensor-split",
    "1,1",
    "-ngl",
    "all",
    "--fit",
    "off",
    "--kv-offload",
    "--flash-attn",
    "on",
    "--ctx-size",
    "262144",
    "--parallel",
    "1",
    "--cache-type-k",
    "q8_0",
    "--cache-type-v",
    "q8_0",
    "--batch-size",
    "1024",
    "--ubatch-size",
    "448",
    "--threads",
    "8",
    "--seed",
    "42",
    "--reasoning",
    "off",
    "--cache-prompt",
    "--slot-prompt-similarity",
    "0.0",
    "--cache-ram",
    "8192",
    "--cache-idle-slots",
    "--kv-unified",
    "--ctx-checkpoints",
    "8",
    "--checkpoint-min-step",
    "8192",
    "--n-predict",
    "128",
    "--timeout",
    "7200",
    "--metrics",
    "--host",
    "127.0.0.1",
    "--port",
    "18091"
  ],
  "note": "Example configuration. State what this profile is for and how it differs from the others (one experimental axis at a time).",
  "vram_note": "Fill in from a smoke/ladder run: KV buffer, model/compute buffers, peak VRAM and headroom per device."
}
